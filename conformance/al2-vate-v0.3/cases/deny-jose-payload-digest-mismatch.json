{
  "version": "vate-conformance-0.3",
  "profile": "VATE-AL2-Verifier-Admission-v0.3",
  "case_id": "deny-jose-payload-digest-mismatch",
  "title": "Deny detached JWS fixture with payload digest mismatch",
  "category": "negative",
  "purpose": "A verifier should fail closed when the detached payload bytes do not match the digest declared by the proof package.",
  "artifacts": {
    "admission_receipt": "examples/receipts/admission-deny-jose-payload-digest-mismatch.example.json",
    "jose_proof": "examples/jose/jose-detached-runtime-attestation-payload-digest-mismatch.example.json",
    "detached_payload": "examples/jose/detached-runtime-attestation-payload.json",
    "trust_bundle": "examples/trust-bundle.example.json"
  },
  "jose_checks": [
    {
      "proof_package": "jose_proof",
      "detached_payload": "detached_payload",
      "trust_bundle": "trust_bundle",
      "checked_at": "2026-07-01T00:18:05Z",
      "expected_typ": "vate-evidence+jws",
      "expect_valid": false,
      "expected_failure_reason": "DIGEST_MISMATCH"
    }
  ],
  "expected": {
    "admission_decision": "deny",
    "should_execute": false,
    "reason_codes": [
      "DIGEST_MISMATCH",
      "FAIL_CLOSED"
    ],
    "checks": [
      {
        "name": "decision.outcome",
        "expected": "fail"
      },
      {
        "name": "evidence.verification.failure_reason",
        "expected": "present"
      }
    ]
  },
  "validation_focus": [
    "detached JWS fixture",
    "payload digest binding",
    "fail closed"
  ]
}
