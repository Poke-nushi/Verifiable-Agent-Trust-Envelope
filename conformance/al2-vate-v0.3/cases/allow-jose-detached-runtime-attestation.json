{
  "version": "vate-conformance-0.3",
  "profile": "VATE-AL2-Verifier-Admission-v0.3",
  "case_id": "allow-jose-detached-runtime-attestation",
  "title": "Allow byte-bound detached JWS runtime attestation fixture",
  "category": "positive",
  "purpose": "A verifier should accept a detached JWS fixture when protected header, payload digest, signing input, and trust bundle constraints all match.",
  "artifacts": {
    "admission_receipt": "examples/receipts/admission-allow.example.json",
    "jose_proof": "examples/jose/jose-detached-runtime-attestation-valid.example.json",
    "detached_payload": "examples/jose/detached-runtime-attestation-payload.json",
    "trust_bundle": "examples/trust-bundle.example.json"
  },
  "jose_checks": [
    {
      "proof_package": "jose_proof",
      "detached_payload": "detached_payload",
      "trust_bundle": "trust_bundle",
      "checked_at": "2026-07-01T00:15:05Z",
      "expected_typ": "vate-evidence+jws",
      "expect_valid": true
    }
  ],
  "expected": {
    "admission_decision": "allow",
    "should_execute": true,
    "reason_codes": [
      "EVIDENCE_VERIFIED",
      "POLICY_MATCH"
    ],
    "checks": [
      {
        "name": "decision.outcome",
        "expected": "pass"
      },
      {
        "name": "jose.detached_payload_digest",
        "expected": "pass"
      },
      {
        "name": "jose.signing_input",
        "expected": "pass"
      }
    ]
  },
  "validation_focus": [
    "detached JWS fixture",
    "byte-level payload binding",
    "trust bundle"
  ]
}
