{
  "version": "vate-conformance-0.3",
  "profile": "VATE-AL2-Verifier-Admission-v0.3",
  "case_id": "deny-attenuation-malformed-money",
  "title": "Deny malformed attenuation money object",
  "category": "negative",
  "purpose": "A verifier should fail closed when an emitted admission receipt attenuation records a malformed money object for max_amount.",
  "artifacts": {
    "admission_receipt": "examples/receipts/admission-deny-attenuation-malformed-money.example.json",
    "bad_attenuation": "conformance/al2-vate-v0.3/fixtures/attenuation-malformed-money.json"
  },
  "attenuation_checks": [
    {
      "artifact": "bad_attenuation",
      "expect_valid": false,
      "expected_failure_reason": "SCHEMA_INVALID"
    }
  ],
  "expected": {
    "admission_decision": "deny",
    "should_execute": false,
    "reason_codes": [
      "SCHEMA_INVALID",
      "FAIL_CLOSED"
    ],
    "checks": [
      {
        "name": "decision.outcome",
        "expected": "fail"
      },
      {
        "name": "evidence.verification.failure_reason",
        "expected": "present"
      }
    ]
  },
  "validation_focus": [
    "attenuation canonicalization",
    "money object shape",
    "fail closed"
  ]
}
