{
  "version": "vate-conformance-0.3",
  "profile": "VATE-AL2-Verifier-Admission-v0.3",
  "case_id": "deny-attenuation-max-amount-type-edge",
  "title": "Deny non-scalar attenuated max amount",
  "category": "negative",
  "purpose": "A verifier should fail closed when an attenuated max_amount value is represented as a non-scalar object instead of a finite decimal amount.",
  "artifacts": {
    "admission_receipt": "examples/receipts/admission-deny-attenuation-max-amount-type-edge.example.json",
    "bad_attenuation": "conformance/al2-vate-v0.3/fixtures/attenuation-max-amount-type-edge.json"
  },
  "attenuation_checks": [
    {
      "artifact": "bad_attenuation",
      "expect_valid": false,
      "expected_failure_reason": "SCHEMA_INVALID"
    }
  ],
  "expected": {
    "admission_decision": "deny",
    "should_execute": false,
    "reason_codes": [
      "SCHEMA_INVALID",
      "FAIL_CLOSED"
    ],
    "checks": [
      {
        "name": "decision.outcome",
        "expected": "fail"
      },
      {
        "name": "evidence.verification.failure_reason",
        "expected": "present"
      }
    ]
  },
  "validation_focus": [
    "attenuation boundary",
    "schema type edge",
    "fail closed"
  ]
}
