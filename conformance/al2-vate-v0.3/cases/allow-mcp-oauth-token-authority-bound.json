{
  "version": "vate-conformance-0.3",
  "profile": "VATE-AL2-Verifier-Admission-v0.3",
  "case_id": "allow-mcp-oauth-token-authority-bound",
  "title": "Allow token evidence with matched upstream authority",
  "category": "positive",
  "purpose": "A verifier should allow an MCP OAuth-shaped request when token presence is paired with matching upstream authorization, resource binding, actor, action class, freshness window, and local policy.",
  "artifacts": {
    "admission_request": "examples/transport/mcp-oauth-token-authority-bound-admission-request.example.json",
    "admission_receipt": "examples/receipts/admission-allow-mcp-oauth-token-authority-bound.example.json"
  },
  "expected": {
    "admission_decision": "allow",
    "should_execute": true,
    "reason_codes": [
      "EVIDENCE_VERIFIED",
      "POLICY_MATCH"
    ],
    "checks": [
      {
        "name": "decision.outcome",
        "expected": "pass"
      },
      {
        "name": "request.transport.oauth.token_present",
        "expected": "present"
      },
      {
        "name": "request.transport.oauth.upstream_authorization",
        "expected": "present"
      },
      {
        "name": "request.transport.oauth.resource_binding",
        "expected": "present"
      },
      {
        "name": "request.transport.oauth.resource",
        "expected": "present"
      },
      {
        "name": "request.target_resource",
        "expected": "present"
      },
      {
        "name": "request.transport.openid.subject",
        "expected": "present"
      },
      {
        "name": "request.transport.mcp.requested_tool_class",
        "expected": "present"
      },
      {
        "name": "evidence.verification.result",
        "expected": "pass"
      }
    ]
  },
  "validation_focus": [
    "MCP transport binding",
    "OAuth authority intersection",
    "resource binding",
    "positive control"
  ]
}
