{
  "version": "vate-conformance-0.2",
  "profile": "VATE-AL2-Verifier-Admission-v0.2",
  "case_id": "deny-attenuation-negative-amount",
  "title": "Deny negative attenuated max amount",
  "category": "negative",
  "purpose": "A verifier should fail closed when an attenuated max_amount value is negative instead of a finite non-negative decimal amount.",
  "artifacts": {
    "admission_receipt": "examples/receipts/admission-deny-attenuation-negative-amount.example.json",
    "bad_attenuation": "conformance/al2-vate-v0.2/fixtures/attenuation-negative-amount.json"
  },
  "attenuation_checks": [
    {
      "artifact": "bad_attenuation",
      "expect_valid": false,
      "expected_failure_reason": "SCHEMA_INVALID"
    }
  ],
  "expected": {
    "admission_decision": "deny",
    "should_execute": false,
    "reason_codes": [
      "SCHEMA_INVALID",
      "FAIL_CLOSED"
    ],
    "checks": [
      {
        "name": "decision.outcome",
        "expected": "fail"
      },
      {
        "name": "evidence.verification.failure_reason",
        "expected": "present"
      }
    ]
  },
  "validation_focus": [
    "attenuation boundary",
    "amount boundary",
    "fail closed"
  ]
}
